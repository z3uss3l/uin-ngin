{
  "name": "uin-ngin",
  "private": true,
  "workspaces": ["packages/*"],
  "devDependencies": {
    "lerna": "^8.1.2"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "clean": "lerna clean"
  }
}
