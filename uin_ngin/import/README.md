# Legacy import package

Use `uin_ngin.image_import` in new code. The directory name `import` is retained for compatibility with older source layouts, but cannot be referenced with normal Python `from ... import ...` syntax because `import` is a Python keyword.
